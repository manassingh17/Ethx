import type { Metadata } from "next";
import { Inter, Space_Grotesk } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Ethical Xchange | Infrastructure for the World's Data Economies",
  description:
    "Ethical Xchange is the infrastructure platform powering radically different data ecosystems across industries. Deploy your own marketplace on blockchain-secured infrastructure.",
  keywords: [
    "data marketplace",
    "enterprise data",
    "blockchain",
    "data infrastructure",
    "white-label marketplace",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${spaceGrotesk.variable}`}>
      <body>{children}</body>
    </html>
  );
}
